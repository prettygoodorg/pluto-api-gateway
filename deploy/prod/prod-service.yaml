apiVersion: v1
kind: Service
metadata:
  name: api-gw-service
  namespace: pluto
spec:
  type: ClusterIP
  selector:
    app: api-gw
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
