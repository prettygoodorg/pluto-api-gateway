apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gw-deployment
  namespace: pluto
  labels:
    app: api-gw
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gw
  template:
    metadata:
      labels:
        app: api-gw
    spec:
      containers:
        - name: api-gw
          image: ghcr.io/prettygoodorg/pluto-api-gateway:prod-latest
          ports:
            - containerPort: 8000
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8000
            initialDelaySeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8000
            initialDelaySeconds: 20
      imagePullSecrets:
        - name: ghcr-secret
